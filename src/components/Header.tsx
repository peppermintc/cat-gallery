import styled from "styled-components";

const Heading = styled.h1`
  margin: 0.67em 0;
`;

const Header = () => {
  return <Heading>😸 고양이 사진 갤러리 😻</Heading>;
};

export default Header;
